# PhysioNet PICS dataset (large; download via picsdb/_download.py)
picsdb/*.dat
picsdb/*.hea
picsdb/*.atr
picsdb/*.qrsc
picsdb/*.resp
picsdb/RECORDS
picsdb/ANNOTATORS
picsdb/.neosense_cache/

# Trained model cache
**/.neosense_cache/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
.env
.env.local
.env.*.local
.env.production

# Smoke tests (local dev only)
backend/_smoke_test.py

# OS / IDE
.DS_Store
Thumbs.db
.vscode/

# Local-only HF deploy helper (never commit)
push-to-hf.ps1

# Stray Apnea-ECG sample files that don't belong here (cleaned from history)
a01.*
a01r.*

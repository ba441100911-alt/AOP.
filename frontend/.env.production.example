# Copy these into Vercel > Project Settings > Environment Variables (Production).
# Replace neosense-backend.onrender.com with your actual Render service URL.

VITE_NEOSENSE_WS_URL=wss://neosense-backend.onrender.com/ws/nicu

# Optional: if you prefer HTTP polling over WebSocket, uncomment:
# VITE_NEOSENSE_STREAM_URL=https://neosense-backend.onrender.com/api/frame
